import os
import sys

# додаєм корінь проєкту в sys.path
sys.path.insert(0, os.path.abspath("."))

from tools.report_builder import create_report

lab_dir = os.path.join("digital_information_processing", "lab1")
plots_dir = os.path.join(lab_dir, "plots")
main_code_path = os.path.join(lab_dir, "main.py")

with open(main_code_path, "r", encoding="utf-8") as f:
    lines = f.readlines()

code_chunk_1 = "".join(lines[0:59])
code_chunk_2 = "".join(lines[59:97])
code_chunk_3 = "".join(lines[97:132])
code_chunk_4 = "".join(lines[132:196])
code_chunk_5 = "".join(lines[196:232])

console_output = """============================================================
Лабораторна робота №1: Детерміновані сигнали для ЦОІ
Виконав: Абдулханов Абдул-Рахім (ФеП-33)
Викладач: проф. Половинко І. І.
============================================================

[1] Прямокутний імпульс Rect(t) та Фур'є-образ sinc(f)
- Формула: rect(t) = 1 при |t| <= tau/2, інакше 0
- Фур'є-образ: F{rect(t)} = tau * sinc(tau * f) = sin(pi * tau * f) / (pi * f)
- Практичне значення: тактові імпульси, двійкові біти в шинах даних, стробування
- Перші нулі спектра: f = +-1.0 Гц (обернено пропорційно тривалості tau)

[2] Гармонічний косинусоїдальний сигнал cos(2*pi*f0*t)
- Формула: s(t) = cos(2*pi*f0*t)
- Фур'є-образ: F{cos} = 0.5 * delta(f + f0) + 0.5 * delta(f - f0)
- Практичне значення: опорні коливання гетеродина, несучі частоти в радіозв'язку
- Спектр локалізований: енергія сконцентрована точно на +- 2.0 Гц

[3] Дельта-функція Дірака delta(t) та нескінченний спектр
- Формула: delta(t) = нескінченність при t=0, інтеграл = 1
- Фур'є-образ: F{delta(t)} = 1 (рівномірний неперервний спектр на всіх частотах)
- Практичне значення: вимірювання імпульсної характеристики h(t) лінійних фільтрів
- Властивість: нульова тривалість у часі дає нескінченно широку смугу частот

[4] Функція знаку sgn(t)
- Формула: sgn(t) = +1 (t>0), 0 (t=0), -1 (t<0)
- Фур'є-образ: F{sgn(t)} = 1 / (j * pi * f), модуль: |F| = 1 / (pi * |f|)
- Практичне значення: перетворення Гільберта, фазові детектори, формування SSB сигналів
- Особливість: неінтегровна функція, енергія спадає гіперболічно ~ 1/f

[5] Трикутний імпульс tri(t) та Фур'є-образ sinc^2(f)
- Формула: tri(t) = 1 - |t|/tau при |t| <= tau, інакше 0
- Фур'є-образ: F{tri(t)} = tau * sinc^2(tau * f)
- Практичне значення: віконна фільтрація (вікно Бартлетта), інтерполяція, згладжування
- Порівняння з rect: бічні пелюстки sinc^2 спадають швидше (~1/f^2 проти 1/f у rect)

[6] Функція відліків sinc(t) та прямокутний фільтр
- Формула: sinc(t) = sin(pi * t) / (pi * t)
- Фур'є-образ: F{sinc(2*B*t)} = (1 / 2B) * rect(f / 2B)
- Практичне значення: теорема Найквіста-Котельникова (ідеальна дискретизація), brick-wall ФНЧ
- Властивість дуальності: прямокутник у часі дає sinc у частоті, а sinc у часі дає прямокутник у частоті"""

goal = "вивчити детерміновані сигнали та освоїти методи їх опрацювання."

theory = """Передача повідомлень у каналах зв’язку здійснюється за допомогою сигналів. Сигнал - фізичний процес або явище, що несе інформацію про подію, стан об’єкта чи команду керування. Інформаційні параметри сигналу: амплітуда, частота, фаза, тривалість, ширина спектра, час затримки, напрям поширення, поляризація.

Основні детерміновані сигнали та їхні перетворення Фур'є:
1. Періодичні синусоїдальні та косинусоїдальні сигнали: F{cos(2*pi*f0*t)} = 0.5*delta(f + f0) + 0.5*delta(f - f0). Спектр строго зосереджений на несучій частоті.
2. Дельта-функція Дірака delta(t): нескінченний імпульс нульової тривалості з площею 1; її спектр є рівномірним на всій осі частот F{delta(t)} = 1.
3. Прямокутна функція rect(t): дорівнює 1 при |t| <= tau/2 та 0 поза цим інтервалом; її спектром є функція sinc: F{rect(t)} = tau*sinc(tau*f).
4. Функція знаку sgn(t): змінює знак залежно від аргументу (-1 при t<0, +1 при t>0); модуль спектра спадає як 1/(pi*|f|).
5. Трикутна функція tri(t): має лінійне зростання і спадання, спектр описується квадратом sinc-функції: F{tri(t)} = tau*sinc^2(tau*f).
6. Функція відліків sinc(t): sin(pi*t)/(pi*t), спектром якої є ідеальне прямокутне вікно частот (основа теореми відліків Найквіста-Котельникова)."""

steps = [
    ("text", "1. Аналіз використання детермінованих сигналів в інформаційних системах:"),
    ("text", "- Гармонічні сигнали (синус/косинус) використовуються як опорні коливання гетеродина, несучі хвилі в радіо- та оптоволоконному зв'язку, а також для частотного розділення каналів (FDM).\n"
             "- Прямокутні імпульси (rect) є основою двійкової передачі інформації (TTL-рівні, цифрові тактові шини процесорів, стробування аналогових комутаторів).\n"
             "- Дельта-функція Дірака застосовується для математичного тестування каналів зв'язку та експериментального визначення імпульсної характеристики лінійних систем.\n"
             "- Функція sinc(t) є базовою інтерполяційною функцією у теоремі Найквіста-Котельникова для ідеального ЦАП-відновлення сигналів без втрат.\n"
             "- Функція signum (sgn) застосовується в детекторах фази, порогових компараторах та алгоритмах перетворення Гільберта (формування односмугової модуляції SSB).\n"
             "- Трикутна функція використовується у формуванні вагових вікон фільтрації (вікно Бартлетта) та системах широтно-імпульсної модуляції (ШІМ)."),

    ("text", "2. Реалізація прямокутного сигналу rect(t) та його аналітичного спектра sinc(f):"),
    ("code_screenshot", code_chunk_1),
    ("image", os.path.join(plots_dir, "1_rect_and_sinc.png")),

    ("text", "3. Моделювання гармонічного сигналу cos(2*pi*f0*t) та спектральних дельта-піків:"),
    ("code_screenshot", code_chunk_2),
    ("image", os.path.join(plots_dir, "2_cosine_spectrum.png")),

    ("text", "4. Моделювання дельта-функції Дірака та рівномірного спектра:"),
    ("code_screenshot", code_chunk_3),
    ("image", os.path.join(plots_dir, "3_dirac_delta.png")),

    ("text", "5. Моделювання функцій signum sgn(t) та трикутного імпульсу tri(t):"),
    ("code_screenshot", code_chunk_4),
    ("image", os.path.join(plots_dir, "4_signum_function.png")),
    ("image", os.path.join(plots_dir, "5_triangular_sinc2.png")),

    ("text", "6. Моделювання функції відліків sinc(t) у часі та прямокутного частотного вікна:"),
    ("code_screenshot", code_chunk_5),
    ("image", os.path.join(plots_dir, "6_sinc_time_rect_spec.png")),

    ("text", "7. Зведений текстовий вивід розрахунку параметрів сигналів:"),
    ("console_screenshot", console_output)
]

conclusion = (
    "У ході лабораторної роботи вивчено основні детерміновані сигнали та їхні спектральні образи, "
    "що використовуються в цифрових інформаційних системах. "
    "На практиці підтверджено властивість дуальності перетворення Фур'є: часова локалізація сигналу "
    "призводить до розширення його спектральної смуги (дельта-імпульс має нескінченний білий спектр, "
    "а прямокутний імпульс скінченної тривалості породжує нескінченний sinc-спектр). "
    "Отримано навички математичного моделювання детермінованих сигналів у Python та досліджено "
    "їхні амплітудно-частотні характеристики, необхідні для проектування цифрових фільтрів і систем зв'язку."
)

output_docx = os.path.join(lab_dir, "lab1_report.docx")
print("Building report...")
docx_file, pdf_file = create_report(
    subject_key="digital_information_processing",
    lab_number=1,
    lab_title="ДЕТЕРМІНОВАНІ СИГНАЛИ ДЛЯ ОБРОБКИ ІНФОРМАЦІЇ",
    goal=goal,
    theory=theory,
    steps=steps,
    conclusion=conclusion,
    output_docx_path=output_docx,
    ai_box=None,
    convert_to_pdf=True
)

print(f"DONE! DOCX: {docx_file}, PDF: {pdf_file}")
